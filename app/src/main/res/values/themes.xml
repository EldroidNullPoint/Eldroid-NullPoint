<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Eldroid_NullPoint" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_dark_green</item>
        <item name="colorPrimaryDark">@color/brand_dark_green</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/brand_green</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Eldroid_NullPoint" parent="Base.Theme.Eldroid_NullPoint" />

    <!--
        Landing-page-only variant. The landing screen is the one screen that uses
        the full-bleed brand dark green, so its system bars have to match it.
        Applied to LandingActivity in the manifest only - every other screen keeps
        the white base theme above.
    -->
    <style name="Theme.Eldroid_NullPoint.Landing">
        <item name="android:windowBackground">@color/brand_dark_green</item>
        <item name="android:statusBarColor">@color/brand_dark_green</item>
        <item name="android:navigationBarColor">@color/brand_dark_green</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
