<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/eldroid"
        android:label="@string/app_name"
        android:roundIcon="@drawable/eldroid"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eldroid_NullPoint"
        tools:targetApi="31">


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Eldroid_NullPoint">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Landing is the only screen using the full dark-green theme variant. -->
        <activity
            android:name=".LandingActivity"
            android:exported="false"
            android:theme="@style/Theme.Eldroid_NullPoint.Landing" />

        <!-- "How it works" tutorial, entered from Landing's Get Started button. -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="false" />

        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".SignupActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ChangePasswordActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <activity
            android:name=".EquipmentListActivity"
            android:exported="false" />

        <activity
            android:name=".ActivityHistoryActivity"
            android:exported="false" />

        <activity
            android:name=".EquipmentDetailActivity"
            android:exported="false" />


    </application>

</manifest>
